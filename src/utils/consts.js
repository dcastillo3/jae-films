const apiDirectory = '/api';

const apis = {
    copy: `${apiDirectory}/copy`,
    videos: `${apiDirectory}/videos`,
};

const initialStates = {
    copy: [],
    videos: [],
}

export {
    apis,
    initialStates
};